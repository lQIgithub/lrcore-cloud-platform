<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.lrcore</groupId>
        <artifactId>lrcore-base-modules</artifactId>
        <version>v1.0.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>lrcore-auth</artifactId>
    <name>auth认证授权中心-业务实现</name>
    <description>
        lrcore-auth认证授权中心
    </description>

    <dependencies>

        <!-- Lrcore Common Auth -->
        <dependency>
            <groupId>com.lrcore</groupId>
            <artifactId>lrcore-common-auth</artifactId>
        </dependency>

        <!-- Lrcore System API -->
        <dependency>
            <groupId>com.lrcore</groupId>
            <artifactId>lrcore-system-api</artifactId>
            <version>${lrcore.version}</version>
        </dependency>

        <!-- SSO OAuth2 授权服务器 JDBC 持久化（lrcore.auth.jdbc.enabled=true 时启用；
             开发默认内存兜底，此依赖仅为生产 JDBC 路径提供运行时类）。
             注意：本服务启动类排除了 DataSourceAutoConfiguration，
             数据源由 LrcoreSsoJdbcConfiguration 按 Nacos 配置显式装配 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Spring 7：WebTestClient 位于 spring-test，但其运行期依赖 webflux 的 reactive 客户端 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webflux</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- SSO 全链路集成测试：H2 内存库承载 OAuth2 三表（官方 DDL） -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
